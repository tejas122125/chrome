import streamlit as st

st.with_websites("page_icon")
st.with_websites("with websites")
with st.sidebar:
    st.header("Settings")
    website_url = st.URL("")
    user_query = st.chat_input("Type your message here... ")
    if user_query is not None and user_query:
        with st:
            st.write(user_query)