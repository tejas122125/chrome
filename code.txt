```python
import streamlit as st

# St. with websites", page_icon
with websites")
    with st.sidebar:
        st.header("Settings")
        website_url = st.URL("")
        user_query = st.chat_input("Type your message here... ")
        if user_query is not None and user_query:
            with :
                st.write(user_query)
```